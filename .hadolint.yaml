# hadolint configuration for tcwlab/betterlint
#
# DL3018: Pin versions in apk add
# Alpine images deliberately do not pin apk package versions: they treat
# the Alpine repository as the source of truth and update only via
# `apk upgrade`. Pinning would break with each Alpine release.
ignore:
  - DL3018
